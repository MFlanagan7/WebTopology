class Modal {
    constructor(options) {
        this.title = options.title || 'Modal Window';
        this.textColor = options.textColor || '#FFF8F0';
        this.bgColor1 = options.bgColor1 || '#333333';
        this.bgColor2 =  options.bgColor2 || '#8693AB';
        this.formFields = [];
    }
}